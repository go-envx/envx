environments: [development, staging, production]

settings:
  overload: false
  strict: false

projects:
  api-core:
    includes:
      - env/postgres
      - env/gateway
      - apps/api-core/env/api-core
  web:
    includes:
      - env/gateway
      - apps/web/env/web
