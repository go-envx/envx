package generate

// -------------------------------------------------------------------------------------

// actionParams are the shared inputs to the keypair generation workflows.
type actionParams struct {
	// Group identifies the secret group receiving the generated keypair.
	Group string
}
