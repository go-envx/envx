// Package diff implements "envx diff <project> <env-a> <env-b>"
//
// It resolves the same project under two environments and compares them.
// Values are masked by default; --reveal opts into plaintext.
package diff
